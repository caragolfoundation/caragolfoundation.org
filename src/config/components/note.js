// Configuration for the note component

const note = {
  // If the note component should be enabled or not
  enabled: true,

  // Note text with HTML support
  text: `<span class="font-bold">Mission:</span
  > Protect and conserve the environment, as well as support vulnerable populations`,

  // Optional note icon (prefix to the text)
  icon: {
    enabled: true,
    name: 'tabler:star',
  },
};

export default note;
